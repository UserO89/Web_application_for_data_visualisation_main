# Production API endpoint for same-origin deployment (Nginx + Laravel on same domain)
VITE_API_BASE_URL=/api/v1

# Optional for local `vite preview` proxy tooling only:
# VITE_BACKEND_URL=https://api.your-domain.com
